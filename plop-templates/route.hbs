import { Router } from 'express';
import {{camelCase name}}Controller from '../controllers/{{camelCase name}}.controller.js';
import { authMiddleware } from '../middleware/auth.js';

const router = Router();

router.get('/', authMiddleware, {{camelCase name}}Controller.getAll);

export default router;